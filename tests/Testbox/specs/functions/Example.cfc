component extends="testbox.system.BaseSpec" {

	function run() {

		g = application.wo

		describe("Tests that DummyTest", () => {

			it("is Returning True", () => {
				assert("true")
			})
			
		})
	}
}
#summary Returns a HTML select tag with months as options.
#labels method,helper,date

You can use the `month_display` argument to control the display of the option tags. By default the full month names will be used but you can change to show abbreviations or month numbers. 

|| *Name* || *Required* || *Type* || *Default* || *Description* ||
|| month_display || no || string || names || names, numbers or abbreviations ||

*Examples:*

{{{
#monthSelectTag(month_display="abbreviations")#
}}}
#summary setXXX(...)
#labels api,0.9

*Category*:

[ModelObjectFunctions Model Object Functions]

*Description*:

Dynamic shortcut method, replace `XXX` with the name of an association you have set up through [hasOne hasOne()].

*Examples*:

{{{
<!--- a "hasOne" association from "author" to "bio" is required --->
<cfset anAuthor = model("author").findByKey(params.authorId)>
<cfset aBio = model("bio").findByKey(params.bioId)>
<cfset anAuthor.setBio(aBio)>
}}}

{{{
<!--- a "hasOne" association from "author" to "bio" is required --->
<cfset anAuthor = model("author").findByKey(params.authorId)>
<cfset anAuthor.setBio(1)>
}}}

 * [Associations Associations] (chapter)
 * [hasOne hasOne()] (function)
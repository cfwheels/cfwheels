#summary findOneByXAndX(...)
#labels api,1.0

*Category*:

[ModelClassFunctions Model Class Functions]

*Description*:

Dynamic shortcut method, see [findOne findOne()] for more info. Replace `X` with property names.

*Examples*:

{{{
<cfset book = model("book").findOneByReleaseYearAndType("#params.year#,#params.type#")>
=
<cfset book = model("book").findOne(where="releaseYear=#params.year# AND type='#params.type#'")>
}}}

 * DynamicFinders (chapter)
 * [findOne findOne()] (function)
 * [findOneByX findOneByX()] (function)
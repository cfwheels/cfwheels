#summary One-sentence summary of this page.
#labels chapter,0.8

google example
query db for only needed info key principle in wheels
pagination is done thru findAll
page, perPage
handle
count
paginate by objects, not records!
how does the SQL look (SQL server vs mysql)
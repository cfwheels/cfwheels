#summary Listing of all configuration functions.
#labels chapter,0.9.1

These functions should be called from files in the `config` folder.

==Example==

{{{
<cfset set(dataSourceName="myDB")>
}}}

==Functions==

 * [addRoute addRoute()]
 * [get get()]
 * [set set()]
#summary errorMessageOn(objectName,property[prependText,appendText,wrapperElement,class])
#labels api,0.9.1

*Category*:

[ViewHelperFunctions View Helper Functions]

*Description*

Returns the error message, if one exists, on the object's property. If multiple error messages exists, the first one is returned.

*Return Type*:

String

*History*:

-

*Parameters*:

|| *objectName* || string || true || - || The variable name of the object to display the error message for ||
|| *property* || string || true || - || The name of the property (database column) to display the error message for ||
|| *prependText* || string || false ||   || String to prepend to the error message ||
|| *appendText* || string || false ||   || String to append to the error message ||
|| *wrapperElement* || string || false || span || HTML element to wrap the error message in ||
|| *class* || string || false || error-message || CSS class to set on the wrapper element ||

*Examples*:

{{{
<!--- view code --->
<cfoutput>
  <p>#checkBox(objectName="photo", property="isPublic", label="Is public?")#<br />
  #errorMessageOn(objectName="photo", property="isPublic")#</p>
</cfoutput>
}}}

*Related*:

 * [FormHelpersandShowingErrors Form Helpers and Showing Errors] (chapter)
 * [errorMessagesFor errorMessagesFor()] (function)
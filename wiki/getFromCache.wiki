#summary This function retrieves an item from the cache.

|| *Name* || *Required* || *Type* || *Default* || *Description* ||
|| key || yes || string || || the key for the cache item you want to retrieve ||

See the section on [Caching] for information and code examples.
#summary Returns a HTML select tag with hours as options.
#labels method,helper,date

This method returns hours from 0 to 23.

|| *Name* || *Required* || *Type* || *Default* || *Description* ||
|| This method takes no arguments || || || || ||

*Examples:*

{{{
#hourSelectTag()#
}}}
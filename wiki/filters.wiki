#summary filters(through[type,only,except])
#labels api,1.0

*Category*:

[ControllerInitializationFunctions Controller Initialization Functions]

*Description*:

XXX.

*Return Type*:

Void

*History*:

-

*Parameters*:

|| *Name* || *Type* || *Required* || *Default* || *Description* ||
|| *through* || string || true || - || XXX ||
|| *type* || string || false || before || XXX ||
|| *only* || string || false ||   || XXX ||
|| *except* || string || false ||   || XXX ||

*Examples*:

{{{
<cfset filters("restrictAccess")>
}}}

{{{
<cfset filters(through="isLoggedIn,checkIPAddress", except="home,login")>
}}}

*Related*:

 * FiltersandVerification (chapter)
 * [verifies verifies()] (function)
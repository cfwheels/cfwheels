#summary Returns whether the page was called from JavaScript or not.
#labels method,controller

|| *Name* || *Required* || *Type* || *Default* || *Description* ||
|| this method takes no arguments || || || || ||

*Examples:*

{{{
<cfset request_is_ajax = isAjax()>
}}}
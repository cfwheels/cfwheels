#summary update([properties,parameterize])
#labels api,1.0

*Category*:

[ModelObjectFunctions Model Object Functions]

*Description*:

Updates the object with the supplied properties and saves it to the database. Returns true if the object was saved successfully to the database and false otherwise.

*Return Type*:

Boolean

*History*:

-

*Parameters*:

|| *Name* || *Type* || *Required* || *Default* || *Description* ||
|| * properties* || struct || false ||   || See documentation for `new` ||
|| *parameterize* || boolean, string or list || false || #application.settings.update.parameterize# || See documentation for `findAll` ||

*Examples*:

{{{
<cfset post = model("post").findByKey(33)>
<cfset post.update(title="New version of Wheels just released")>
}}}

{{{
<cfset post = model("post").findByKey(params.key)>
<cfset post.update(title="New version of Wheels just released", properties=params.post)>
}}}

*Related*:

 * UpdatingRecords (chapter)
 * [updateByKey updateByKey()] (function)
 * [updateOne updateOne()] (function)
 * [updateAll updateAll()] (function)
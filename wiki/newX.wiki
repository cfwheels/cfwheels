#summary newX(...)
#labels api,1.0

*Category*:

[ModelObjectFunctions Model Object Functions]

*Description*:

Dynamic shortcut method, see [new new()] for more info. Replace `X` with the name of an association you have set up either through [belongsTo belongsTo()], [hasMany hasMany()] or [hasOne hasOne()].

*Examples*:

{{{
<cfset aCustomer = model("customer").findByKey(1)>
<cfset aCustomer.newOrder(shipping=params.shipping)>
=
<cfset model("order").new(customerId=params.customerId, shipping=params.shipping)>
}}}

 * CreatingRecords (chapter)
 * [new new()] (function)
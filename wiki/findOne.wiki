#summary findOne([where,order,select,cache,reload,parameterize])
#labels api,1.0

*Category*:

[ModelClassFunctions Model Class Functions]

*Description*:

Fetches the first record found based on the `WHERE` and `ORDER BY` clauses and returns it as an object. Throws an error if no record is found.

*Return Type*:

Object

*History*:

-

*Parameters*:

|| *Name* || *Type* || *Required* || *Default* || *Description* ||
|| *where* || string or list || false || - || See documentation for `findAll` ||
|| *order* || string or list || false || - || See documentation for `findAll` ||
|| *select* || string || false || - || See documentation for `findAll` ||
|| *cache* || boolean or numeric || false || - || See documentation for `findAll` ||
|| *reload* || boolean || false || #application.settings.get.reload# || See documentation for `findAll` ||
|| *parameterize* || any || false || #application.settings.get.parameterize# || See documentation for `findAll` ||

*Examples*:

{{{
<cfset anOrder = model("order").findOne(order="datePurchased DESC")>
}}}

*Related*:

 * ReadingRecords (chapter)
 * [findByKey findByKey()] (function)
 * [findAll findAll()] (function)
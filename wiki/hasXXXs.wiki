#summary hasXXXs(...)
#labels api,1.0

*Category*:

[ModelObjectFunctions Model Object Functions]

*Description*:

Dynamic shortcut method, see [exists exists()] for more info. Replace `XXX` with the name of an association you have set up through [hasMany hasMany()].

*Examples*:

{{{
<!--- a "hasMany" association from "post" to "comments" is required --->
<cfset aPost = model("post").findByKey(params.postId)>

<cfset postHasComments = aPost.hasComments()>
=
<cfset postHasComments = model("comment").exists(where="postId=#aPost.id#")>
}}}

 * [Associations Associations] (chapter)
 * [ReadingRecords ReadingRecords](chapter)
 * [hasMany hasMany()] (function)
 * [exists exists()] (function)
/**
 * {{targetName}} model spec — generated by the Wheels CLI.
 *
 * This is code you own — extend it as your validations and associations grow.
 * Run with `wheels test` after `wheels migrate latest`.
 */
component extends="wheels.WheelsTest" {

	function run() {

		describe("{{targetName}}", () => {

			it("instantiates a new record", () => {
				var record = model("{{targetName}}").new();
				expect(IsObject(record)).toBeTrue();
			});
{{validationExamples}}
		});

	}

}

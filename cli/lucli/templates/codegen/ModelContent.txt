/**
 * |ObjectNameSingularC| model — generated by the Wheels CLI.
 *
 * This is code you own. Declare validations, associations, callbacks, and
 * query scopes in config(). See the Model Quick Reference in
 * docs/consumer-ai/CLAUDE.md for the available helpers.
 */
component extends="Model" {

	function config() {
		|TableName|
		{{belongsToRelationships}}
		{{hasManyRelationships}}
		{{hasOneRelationships}}
		{{validations}}
		{{enums}}
	}

}

// wheels:generate-auth:services:begin — generated by `wheels generate auth`; re-run with --force to regenerate
// Strategy-registry authenticator + session strategy. Passwords are hashed
// with the framework's bcryptHash()/bcryptVerify() global helpers (no
// service registration needed). All singletons: one instance per application
// lifetime.
local.di = injector();
local.di.map("authenticator").to("wheels.auth.Authenticator").asSingleton();
local.di.map("sessionStrategy").to("wheels.auth.SessionStrategy").asSingleton();
// wheels:generate-auth:services:end

<!--- Generated by `wheels generate auth` (Wheels CLI {{cliVersion}}) on {{generatedDate}}.
      Code you own — edit freely. Re-run with --force and review `git diff` to upgrade. --->
<cfparam name="{{modelVar}}" default="">
<cfoutput>

<h1>Choose a new password</h1>

<cfif IsObject({{modelVar}})>
	#errorMessagesFor("{{modelVar}}")#
</cfif>

#startFormTag(route="password", method="put", key=params.key)#
	<div>
		#passwordFieldTag(name="{{modelVar}}[password]", label="New password (12 characters minimum)")#
	</div>
	<div>
		#passwordFieldTag(name="{{modelVar}}[passwordConfirmation]", label="Confirm new password")#
	</div>
	<div>
		#submitTag(value="Reset password")#
	</div>
#endFormTag()#

</cfoutput>

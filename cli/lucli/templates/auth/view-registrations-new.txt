<!--- Generated by `wheels generate auth` (Wheels CLI {{cliVersion}}) on {{generatedDate}}.
      Code you own — edit freely. Re-run with --force and review `git diff` to upgrade. --->
<cfparam name="{{modelVar}}" default="">
<cfoutput>

<h1>Create your account</h1>

#errorMessagesFor("{{modelVar}}")#

#startFormTag(route="registrations")#
	<div>
		#emailField(objectName="{{modelVar}}", property="email", label="Email")#
	</div>
	<div>
		#passwordField(objectName="{{modelVar}}", property="password", label="Password (12 characters minimum)")#
	</div>
	<div>
		#passwordField(objectName="{{modelVar}}", property="passwordConfirmation", label="Confirm password")#
	</div>
	<div>
		#submitTag(value="Sign up")#
	</div>
#endFormTag()#

<p>Already have an account? #linkTo(route="login", text="Log in")#</p>

</cfoutput>

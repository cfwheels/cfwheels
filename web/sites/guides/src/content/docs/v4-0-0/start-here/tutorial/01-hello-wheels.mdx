---
title: "Part 1: Hello, Wheels"
description: Install Wheels, create your first app, and render a page. No concepts yet — just get something running in 20 minutes.
type: tutorial
sidebar:
  order: 1
---

import { Aside, Steps, FileTree } from '@astrojs/starlight/components';

You'll create a new Wheels app called `blog`, start the dev server, and add a `/hello` route that renders a custom page. By the end, `curl localhost:8080/hello` returns "Hello, World."

**You'll learn:**

- What `wheels new` scaffolds
- How to add a route, a controller action, and a view
- How reload works

<Aside type="note">
  This is Part 1 of the [Build a Blog tutorial](/v4-0-0/start-here/tutorial/). You don't need Wheels or CFML experience — just general programming background.
</Aside>

## Where we left off

This is Part 1. You're starting with nothing. By the end, you'll have a working Wheels app responding to `/hello`. Each subsequent part begins with a "Where we left off" banner showing the file tree and database schema, so you can resume without reading Part N-1.

## Install the Wheels CLI

If you haven't yet, see [Installing Wheels](/v4-0-0/start-here/installing/) for macOS, Windows, and Linux.

<Aside type="tip" title="Already installed? Make sure you're current.">
  The tutorial tracks the current **stable** release. If you installed a while back, refresh first:

  - **macOS / Linux (Homebrew):** `brew update && brew upgrade wheels`
  - **Windows / other:** see [Installing → Upgrading](/v4-0-0/start-here/installing/#upgrading)

  Then re-run `wheels --version` and confirm the version matches the [latest release](https://github.com/wheels-dev/wheels/releases). (Bleeding-edge builds work too, but stable is what the tutorial is verified against — see [Release Channels](/v4-0-0/start-here/release-channels/).)
</Aside>

Verify:

```bash {test:cli cmd="wheels --version" asserts-stdout="Wheels"}
wheels --version
```

You should see a Wheels version number. If not, revisit the install guide.

## Create the app

<Steps>

1. Create a new app called `blog`. Run this in a fresh directory where you want the app to live:

   ```bash title="your shell"
   wheels new blog
   ```

   Expected output: a configuration summary (port, datasource, reload password) and the `Next steps` block.

2. Move into the new directory:

   ```bash title="your shell"
   cd blog
   ```

3. Start the dev server:

   ```bash title="your shell"
   wheels start
   ```

   The server boots in a few seconds and prints the URL it's listening on (default: `http://localhost:8080`).

   <Aside type="note" title="First start downloads ~74 MB">
     The very first `wheels start` on a fresh machine downloads Lucee Express (~74 MB). You'll see a progress bar; expect ~30 seconds on a typical connection. Subsequent starts skip the download and boot immediately.
   </Aside>

</Steps>

Open `http://localhost:8080` in your browser. You'll see the starter page: a **Welcome to blog** heading, the runtime details (Wheels version, engine, datasource, environment), and cards linking to the dev tools — guides, routes, the migrator, tests, system info, and the API reference.

It exists so a fresh app boots to something useful instead of a blank page. It is deliberately not a real homepage, and it prints your engine and datasource names — replace `app/views/main/index.cfm` before you deploy.

## What got created

<FileTree>
- blog
  - app
    - controllers
      - Main.cfc
    - events
    - global
    - jobs
    - lib
    - mailers
    - migrator
      - migrations
    - models
    - plugins
    - snippets
    - views
      - helpers.cfm
      - layout.cfm
      - main
        - index.cfm
  - config
    - app.cfm
    - environment.cfm
    - routes.cfm
    - settings.cfm
  - db
    - development.sqlite
    - test.sqlite
  - public
    - Application.cfc
    - index.cfm
    - urlrewrite.xml
    - files
    - images
    - javascripts
    - miscellaneous
    - stylesheets
  - rewrite.config
  - tests
    - specs
      - controllers
      - functional
      - models
  - vendor
    - wheels
  - .env
  - .gitignore
  - lucee.json
</FileTree>

Three directories matter right now:

- `app/controllers/` — request-handling code. You'll see the default `Main.cfc`.
- `app/views/` — templates. The default home page lives at `app/views/main/index.cfm`.
- `config/routes.cfm` — maps URLs to controller actions.

`vendor/wheels/` is the framework itself, copied into your app. You generally don't touch it.

The other top-level `app/` directories (`events/`, `global/`, `jobs/`, `lib/`, `mailers/`, `migrator/`, `models/`, `plugins/`, `snippets/`) are scaffolding for features you'll meet later — background jobs, mailers, application events, model definitions, generator templates. Leave them alone for now. The remaining root-level files are framework configuration: `lucee.json` configures the Lucee runtime, `.env` holds environment variables (the dev server's reload password lives here), and `config/app.cfm` plus the rest of `config/` configure the framework itself.

<Aside type="tip">
  **On styling:** the generated `app/views/layout.cfm` includes [simple.css](https://simplecss.org/) via CDN — a classless stylesheet that gives semantic HTML a clean, modern look without any markup changes. Wheels' form helpers (`#textField()#`, `#startFormTag()#`, etc.) already emit the right tags, so your scaffolded views render polished from the very first page. Remove the `<link>` if you're bringing your own CSS. For a fuller component kit with proper buttons, cards, and form helpers, see the [optional bonus chapter](/v4-0-0/start-here/tutorial/08-bonus-basecoat/) at the end of the tutorial — it walks through installing `wheels-basecoat`.

  **On Turbo:** a fresh `wheels new` ships with only the core framework. Turbo (for SPA-like page transitions and inline form errors) lands in Part 4 via a CDN script tag. For now, everything is plain server-rendered HTML.
</Aside>

## Add a `/hello` route

You'll add a new URL, a new action on the existing `Main` controller, and a new view.

<Steps>

1. Open `config/routes.cfm`. Inside the `<cfscript>...</cfscript>` block, you'll find a `mapper()...end()` chain. It looks like this:

   ```cfm title="config/routes.cfm — starting state"
   mapper()
       // CLI-Appends-Here
       .wildcard()
       .root(to="main##index", method="get")
   .end();
   ```

   Add a `.get(...)` line for `/hello` before `.wildcard()`:

   ```cfm {test:compile}
   mapper()
       .get(name="hello", pattern="/hello", to="main##hello")
       .wildcard()
       .root(to="main##index", method="get")
   .end();
   ```

   `.get(...)` declares: when a GET hits `/hello`, run the `hello` action on the `Main` controller. Route name `hello` means you can generate this URL elsewhere with `urlFor(route="hello")`.

2. Open `app/controllers/Main.cfc` and add a `hello` action alongside `index`:

   ```cfm {test:compile}
   component extends="Controller" {
       function index() {
       }
       function hello() {
       }
   }
   ```

   The action does nothing visible. Wheels will still render the view at `app/views/main/hello.cfm` by convention.

3. Create `app/views/main/hello.cfm`:

   ```cfm title="app/views/main/hello.cfm"
   <h1>Hello, World</h1>
   ```

4. Tell Wheels to pick up your changes:

   ```bash title="your shell"
   wheels reload
   ```

</Steps>

## Checkpoint

Your app should now respond to `/hello`:

```bash title="verify"
curl -s http://localhost:8080/hello
```

Expected: `<h1>Hello, World</h1>` in the response body (plus any layout wrapping).

## Troubleshooting

**`wheels: command not found`** — PATH doesn't see the CLI. See [Installing Wheels](/v4-0-0/start-here/installing/).

**`Route '/hello' not found`** — you added the route but didn't reload. Run `wheels reload`.

**Blank page at `/hello`** — the view file is in the wrong place. It must be at `app/views/main/hello.cfm` (lowercase `main`, matching the controller name).

## What's next

In [Part 2](/v4-0-0/start-here/tutorial/02-first-model/) you'll add your first model, write a migration, and seed real data. The browser-render-a-page part is done; now you meet the database.
